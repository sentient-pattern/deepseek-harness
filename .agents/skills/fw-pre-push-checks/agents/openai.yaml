interface:
  display_name: "DSH Pre-Push Checks"
  short_description: "Run the relevant ForgeWeaver checks before push"
  default_prompt: "Use $fw-pre-push-checks before pushing this ForgeWeaver branch."
