# Session-title snapshot composition: the optional first-prompt provider uses
# the ordinary ForgeWeaver route while the ACP app and every other capability stay
# identical to the base example.
- id: base
  name: '@forgeweaver/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: session-title-provider
            name: '@forgeweaver/fw-session-title-first-prompt-llm'
            config:
              targetWords: 5
              targetCjkCharacters: 10
              maxInputBytes: 4096
              maxOutputTokens: 32
              timeoutMs: 5000
              provider: forgeweaver-official
              model: forgeweaver-v4-flash
