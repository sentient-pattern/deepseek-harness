# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@forgeweaver/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-forgeweaver
        name: '@forgeweaver/fw-llm-forgeweaver'
        disabled: true
      - insert:
          - id: lsp
            name: '@forgeweaver/fw-lsp'
          - id: lsp-stdio
            name: '@forgeweaver/fw-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@forgeweaver/fw-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@forgeweaver/fw-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: '@forgeweaver/fw-llm-replay'
