# Replay counterpart to advanced.cordis.yml. It includes the base `cordis.yml`
# directly — a config patch cannot target an entry behind a nested include — and
# restates advanced.cordis.yml's overlay (the agent and persistence configs plus the
# code-runtime and tool-cordis inserts) so the whole app config lives in one patch.
# It re-pins `forgeweaver-v4-flash`: `cordis.yml` ships `forgeweaver-v4-pro`, but the
# recorded corpus (request headers, provenance) was captured on flash, so replay
# holds the recorded model to stay reproducible without a re-record. It also
# disables the key-requiring ForgeWeaver adapter and inserts `llm-replay` to serve
# recorded JSONL without a key or network.
- id: base
  name: '@forgeweaver/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-forgeweaver
        name: '@forgeweaver/fw-llm-forgeweaver'
        disabled: true
      - id: agent-spine
        name: '@forgeweaver/fw-agent-spine-demo'
        config:
          agents:
            - id: main
              provider: forgeweaver-official
              model: forgeweaver-v4-flash
              cwd: !!js process.cwd()
          workspaceContext:
            maxBytes: 65536
          tools:
            mode: both
          persona: |
            You are headless-agent, a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

            Verify your work by running the code or tests. Keep answers brief and factual.
      - id: persistence
        name: '@forgeweaver/fw-session-persistence-jsonl'
        config:
          root: './.sessions'
          # Replay fixtures are raw JSONL; the whole-config patch must restate
          # the compression choice or the default zstd frames hide the logs.
          compression: none
      - insert:
          - id: code-runtime
            name: '@forgeweaver/fw-code-runtime-worker-thread'
          - id: cordis-host-runner
            name: '@forgeweaver/fw-cordis-host-runner'
          - id: tool-cordis
            name: '@forgeweaver/fw-tool-cordis'
          - id: llm-replay
            name: '@forgeweaver/fw-llm-replay'
