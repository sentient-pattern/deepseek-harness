# Add the persisted goal domain and its model-facing tools to the real one-shot app.
- id: base
  name: '@forgeweaver/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: goal
            name: '@forgeweaver/fw-goal'
          - id: tool-goal
            name: '@forgeweaver/fw-tool-goal'
