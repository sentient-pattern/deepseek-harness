# Keyless Agent Teams composition over the real headless app and deterministic fixture adapter.
- id: base
  name: '@forgeweaver/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-forgeweaver
        name: '@forgeweaver/fw-llm-forgeweaver'
        disabled: true
      - id: tool-subagent-control
        name: '@forgeweaver/fw-tool-subagent-control'
        disabled: true
      - id: tool-subagent-report
        name: '@forgeweaver/fw-tool-subagent-report'
        disabled: true
      - id: tool-subagent
        name: '@forgeweaver/fw-tool-subagent'
        config:
          provider: spawn
          toolName: subagent
          backgroundMode: one-shot
          maxDepth: 1
      - id: tool-subagent-fork
        name: '@forgeweaver/fw-tool-subagent'
        config:
          provider: fork
          toolName: subagent_fork
          backgroundMode: one-shot
          maxDepth: 1
      - insert:
          - id: agent-team
            name: '@forgeweaver/fw-experimental-agent-team'
          - id: tool-agent-team
            name: '@forgeweaver/fw-experimental-tool-agent-team'
          - id: team-fixture-llm
            name: './tests/fixtures/team-llm.mjs'
