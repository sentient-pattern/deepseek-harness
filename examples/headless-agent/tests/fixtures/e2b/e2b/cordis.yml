# One-world invariant (same pairing as examples/headless-agent/e2b.cordis.yml):
# e2b.cwd and sandbox-policy.workspaceRoot must name the same remote directory,
# which is also bash-local's implicit default workdir.
- id: e2b
  name: '@forgeweaver/fw-e2b'
  config:
    cwd: !!js process.cwd()
    timeoutMs: 180000

- id: subprocess-e2b
  name: '@forgeweaver/fw-subprocess-e2b'

- id: bash
  name: '@forgeweaver/fw-bash-local'
  config:
    timeoutMs: 30000

- id: fs-e2b
  name: '@forgeweaver/fw-fs-e2b'

- id: agents
  name: '@forgeweaver/fw-agent'

- id: sandbox-policy
  name: '@forgeweaver/fw-sandbox-policy'
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: pty
  name: '@forgeweaver/fw-terminal'

- id: terminal-bash
  name: '@forgeweaver/fw-terminal-bash'
  config:
    pollIntervalMs: 25
    exactProbeAfterMs: 150
    idleSilenceMs: 2000
    handoffGraceMs: 500
    timeoutMs: 5000
    disposeGraceMs: 1000

- id: lsp
  name: '@forgeweaver/fw-lsp'

- id: lsp-stdio
  name: '@forgeweaver/fw-lsp-stdio'
  config:
    servers:
      fixture:
        command: node
        args:
          - !!js process.cwd() + '/fixture-lsp.mjs'
        extensionToLanguage:
          .ts: typescript
        shutdownTimeoutMs: 1000
        killGraceMs: 500
