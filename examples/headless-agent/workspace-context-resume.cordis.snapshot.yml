# Keyless real-Loader composition for workspace-instruction resume
# reconciliation. The test seeds one persisted baseline, changes AGENTS.md
# while the session is offline, then resumes through the public agent service.

- id: persistence
  name: '@forgeweaver/fw-session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: none

- id: replay
  name: '@forgeweaver/fw-llm-replay'
  config:
    file: !!js process.env.FW_SNAPSHOT_FILE
    overrideFile: !!js process.env.FW_SNAPSHOT_OVERRIDE

- id: fs-local
  name: '@forgeweaver/fw-fs-local'
  config:
    cwd: !!js process.cwd()

- id: agent
  name: '@forgeweaver/fw-agent-spine-demo'
  config:
    agents: []
    workspaceContext:
      maxBytes: 65536
      fwHome: !!js process.cwd() + '/.fw'
    skills:
      enabled: false
    toolBash: false
    toolJobs: false
    goals: false

# Await the persisted resume before the headless driver inspects root agents.
- id: resumed-agent
  name: './tests/fixtures/workspace-context-resume-agent.ts'
