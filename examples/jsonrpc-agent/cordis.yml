# Unattended coding-agent deployment for the bundled fw-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@forgeweaver/fw-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.FW_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.FW_MAX_TOKENS_AS_SUCCESS)"

# The ForgeWeaver adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-forgeweaver
  name: '@forgeweaver/fw-llm-forgeweaver'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@forgeweaver/fw-subprocess-local'

- id: bash
  name: '@forgeweaver/fw-bash-local'
  config:
    cwd: !!js process.env.FW_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@forgeweaver/fw-agent-spine-demo'
  config:
    persona: !!js process.env.FW_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@forgeweaver/fw-session-persistence-jsonl'
  config:
    root: !!js process.env.FW_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.FW_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@forgeweaver/fw-session-checkpoint-policy'

- id: subagent
  name: '@forgeweaver/fw-subagent'

- id: subagent-spawn-in-process
  name: '@forgeweaver/fw-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@forgeweaver/fw-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@forgeweaver/fw-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@forgeweaver/fw-fs-local'
  config:
    cwd: !!js process.env.FW_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@forgeweaver/fw-fs-observation-policy'

- id: tool-fs
  name: '@forgeweaver/fw-tool-fs'

- id: token-meter
  name: '@forgeweaver/fw-token-meter'

- id: compaction-basic
  name: '@forgeweaver/fw-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
