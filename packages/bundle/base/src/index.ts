/**
 * @forgeweaver/fw-base — the shared fw core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `fw.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @forgeweaver/fw-base
 */

export {}
