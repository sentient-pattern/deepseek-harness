# Bundled default config. The runtime still requires an explicit
# $FW_CORDIS_CONFIG or argv path; the SDK injects this path for bundled
# zero-config launches. SDK-set session-root and cwd variables have manual-run fallbacks.

# Stdio JSON-RPC server entry; without it the agent has no SDK client.
- id: sdk-jsonrpc-server
  name: '@forgeweaver/fw-sdk-jsonrpc-server'

# Agent spine; the SDK server creates agents per sessionId.
- id: agent-core
  name: '@forgeweaver/fw-agent-spine-demo'
  config:
    workspaceContext:
      maxBytes: 65536

# Stock ForgeWeaver adapters. The adapter resolves FORGEWEAVER_API_KEY through the
# credential seam and, with no provider mounted here, from the launching
# environment; FORGEWEAVER_BASE_URL follows the same environment ladder. Neither
# is inlined, so this file names no secret and no route.
- id: llm-forgeweaver
  name: '@forgeweaver/fw-llm-forgeweaver'

# JSONL persistence; $FW_SESSION_ROOT wins over ./.sessions in the process cwd.
- id: sessions
  name: '@forgeweaver/fw-session-persistence-jsonl'
  config:
    root: !!js process.env.FW_SESSION_ROOT ?? './.sessions'

# Persistence owns durable storage; this separate policy explicitly selects
# the request, tool-dispatch, and completed-step durability checkpoints.
- id: session-checkpoints
  name: '@forgeweaver/fw-session-checkpoint-policy'

# Local bash executor; $FW_CWD wins over the process cwd.
# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@forgeweaver/fw-subprocess-local'

- id: bash
  name: '@forgeweaver/fw-bash-local'
  config:
    cwd: !!js process.env.FW_CWD ?? process.cwd()

# Local filesystem provider for workspace instruction loading. This does not
# expose model-facing file tools by itself.
- id: fs-local
  name: '@forgeweaver/fw-fs-local'
  config:
    cwd: !!js process.env.FW_CWD ?? process.cwd()
